# Data

The raw COMPAS CSV is not bundled in this upload package by default.

Run:

```bash
python3 reproduce_compas_section4_2.py
```

The script will download `compas-scores-two-years.csv` from ProPublica:
https://github.com/propublica/compas-analysis
